{
  "name": "Untitled GraphQL Schema",
  "extensions": {
    "endpoints": {
      "Directus Data Endpoint": {
        "schemaPath": "graphql/data.graphql",
        "url": "${env:NEXT_PUBLIC_DIRECTUS_URL}/graphql",
        "headers": {
          "user-agent": "JS GraphQL",
          "Authorization": "Bearer ${env:DIRECTUS_ACCESS_TOKEN}"
        },
        "introspect": true
      },
      "Directus Management Endpoint": {
        "schemaPath": "graphql/admin.graphql",
        "url": "${env:NEXT_PUBLIC_DIRECTUS_URL}/graphql/system",
        "headers": {
          "user-agent": "JS GraphQL",
          "Authorization": "Bearer ${env:DIRECTUS_ACCESS_TOKEN}"
        },
        "introspect": true
      }
    }
  }
}